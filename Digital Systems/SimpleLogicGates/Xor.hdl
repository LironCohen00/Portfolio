/**
 * Exclusive-or gate:
 * out = not (a == b)
 */

CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    // Put your code here:
	Or(a=a,b=b, out=aOrb);
	Nand(a=a,b=b, out=aAndb);
	And(a=aOrb, b=aAndb, out=out);
}