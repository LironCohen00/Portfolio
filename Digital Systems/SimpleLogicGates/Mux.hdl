/** 
 * Multiplexor:
 * out = a if sel == 0
 *       b otherwise
 */

CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
    // Put your code here:
	Not (in=sel, out=nsel);
	And(a=a,b=nsel, out=aAndNsel);
	And(a=b,b=sel,out=aAndSel);
	Or(a=aAndNsel, b= aAndSel, out=out);
}